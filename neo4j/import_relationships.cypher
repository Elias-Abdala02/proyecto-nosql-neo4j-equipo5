// Import Relationships
// This file contains Cypher queries to create relationships between nodes

// Create relationships
// Example:
// MATCH (c:Customer {id: 'customer_id'})
// MATCH (p:Product {id: 'product_id'})
// CREATE (c)-[:PURCHASED]->(p);
